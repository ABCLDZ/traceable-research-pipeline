# Example only. The application does not load this file automatically.
# Export these values in the shell that runs research-flow.
DEEPSEEK_API_KEY=replace-me
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
# Optional. Set these from the provider's current price sheet.
DEEPSEEK_INPUT_COST_PER_M=0
DEEPSEEK_OUTPUT_COST_PER_M=0
